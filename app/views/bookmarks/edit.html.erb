<h1 class="text-center">Edit Bookmark</h1>

<div class="container-fluid">
  <h4 class="text-left">Enter your new bookmark URL<h4>
    <br />
  <%= form_for [@bookmark.topic, @bookmark] do |f| %>
    <div class="form-group">
      <%= f.label :url %>
      <%= f.text_field :url, class: 'form-control', placeholder: "Enter Topic Name", autofocus: true %>
    </div>
    <%= f.submit "Save", class: 'btn btn-success' %>
  <% end %>
</div>
